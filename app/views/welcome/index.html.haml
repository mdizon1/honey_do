%h1 [PLACEHOLDER] WELCOME TO HONEY DO!!!!! [PLACEHOLDER]

- if user_signed_in?
  - if current_user.household
    = link_to 'Go to your household', household_path(current_user.households.first)
  - else
    = link_to 'Create your household', new_household_path
